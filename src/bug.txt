1.快捷方式提交的json有问题
2.系统日志并没有添加，这个亟须添加，因为开发多了之后再添加需要回头再熟悉业务。
3.goPage没有遵循inMessage格式，因为前后端都写完，暂且不改。
4.首页的菜单导航栏位置偏下bug。。
5.功能列表需要有权限判定，部门判定+权限等级判定，当前用户可以获取的功能。
	  功能表应该加上             -权限   和 - 所属部门  字段
6.每个请求加一个权限判断，request.getSession().getAttribute("pageUser"); 
	初步---先做判空判断
	先判空，再判断部门，然后判断权限等级。
	高级---新建  请求表 请求接口所属的功能id，然后再判断该当前用户的盖茨请求所属的功能其是否有权限求情，无则跳转nopower

	